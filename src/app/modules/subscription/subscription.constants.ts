export enum SubscriptionPlatform {
  GOOGLE = 'google',
  APPLE = 'apple',
}

export enum SubscriptionStatus {
  ACTIVE = 'active',
  EXPIRED = 'expired',
  CANCELED = 'canceled',
  PENDING = 'pending',
}
